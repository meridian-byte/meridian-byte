const imageUrl = `/images`;

export const images = {
  brand: {
    icon: {
      meta: {
        light: `${imageUrl}/brand/icon/meta-light.png`,
        dark: `${imageUrl}/brand/icon/meta-dark.png`,
      },
    },
  },
};
